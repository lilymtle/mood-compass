import { db } from "../config/db";

// adds user mood tracker data
export const addUserMood = (req, res) => {
    try {
        
    } catch (error) {
        
    }
};

// gets user mood tracker data
export const getUserMood = (req, res) => {
    try {
        
    } catch (error) {
        
    }
};

// updates user mood tracker data
export const editUserMood = (req, res) => {
    try {
        
    } catch (error) {
        
    }
};

// deletes user mood tracker data
export const deleteUserMood = (req, res) => {
    try {
        
    } catch (error) {
        
    }
};